export type Vehicle = {
  id: string;
  make: string;
  model: string;
  year: number;
  price: number;
  category: string;
};

export type VehicleInput = {
  make: string;
  model: string;
  year: number;
  price: number;
  category: string;
};
