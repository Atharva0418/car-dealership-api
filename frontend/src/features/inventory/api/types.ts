export type Vehicle = {
  id: string;
  make: string;
  model: string;
  price: number;
  category: string;
  quantityInStock: number;
};

export type VehicleInput = {
  make: string;
  model: string;
  price: number;
  category: string;
  quantityInStock: number;
};
